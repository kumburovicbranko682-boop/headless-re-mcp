package com.headlessre.sample;

/** Declared as a <receiver> in the manifest; a real class so the name resolves. */
public class BootReceiver {
    public boolean enabled() {
        return true;
    }
}
