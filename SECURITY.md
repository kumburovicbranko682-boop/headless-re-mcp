# 安全策略

Headless RE-MCP 是一个**会真实执行未知样本**的本地逆向工程服务：它能启动被调试进程、写寄存器/内存、打补丁、改 APK、驱动浏览器。因此它的安全边界既包括代码本身，也包括你如何部署它。这份文档说明我们承诺的边界、如何私密上报漏洞，以及哪些属于报告范围。

## 支持的版本

在 1.0 之前工具面仍可能在 minor 版本间变化，安全修复只跟进**最新发布的 minor 线**。

| 版本 | 状态 |
|------|------|
| 0.2.x | 维护中，接收安全修复 |
| < 0.2 | 不再维护，请升级 |

## 上报漏洞

**请勿公开开 issue 或 PR 来描述安全问题。** 在修复发布之前公开细节会把用户直接暴露给攻击者。

请走 GitHub 的私密上报通道：仓库
[Security → Report a vulnerability](https://github.com/kumburovicbranko682-boop/headless-re-mcp/security/advisories/new)。
如果该通道对你不可用，可在一个**不含任何漏洞细节**的普通 issue 里请求一个私密联系方式。

一份好的报告包含：

- 受影响的版本、操作系统与依赖后端（IDA / x64dbg / 可选 CLI）；
- 复现步骤，最好带最小样本或最小 MCP 调用序列；
- 影响评估：能读到什么、能改什么、能不能越过读写策略或审批门；
- 如可能，附一份修复或缓解建议。

本项目由单维护者维护、公开历史很短，响应是**尽力而为**，没有 SLA。我们会确认收到、协同复现、在修复就绪后协调公开，并在你同意的前提下在发布说明里致谢。

## 安全模型与边界

设计上有几条硬边界，绕过它们即视为漏洞：

- **无任意命令/求值透传。** 没有 `dynamic.command`、没有 `device.shell`、没有 `web.evaluate`，也不接受调用方自带的 Frida 脚本。每个能力都是具名、参数受校验的工具；动态写操作只接受明确参数与白名单寄存器。设备序列号与包名按严格正则校验以杜绝参数注入。
- **读写策略在调用时强制。** `local_full_access: false` 时，所有会改状态或写文件的工具返回 `write_disabled` 错误信封而不是执行；工具仍然可见，调用方拿到的是能理解的拒绝。守卫下沉在 `CommandCatalog`，所以 MCP、Web 控制台的 agent 路由与 OpenAI 桥接共享同一套策略，任一条 transport 都不能绕过。
- **审批门。** `agent_never_auto_approve` 优先于一切授权；`patches.apply` / `static.bytes.patch` 与 APK/Web 改包默认需要人工批准。绕过这些门自动执行写操作属于漏洞。
- **本地暴露面最小。** Web 监控台默认只监听回环并带 token；`/healthz`、`/readyz`、`/metrics` 刻意不需要 token，但也不返回敏感数据。把控制台转发到局域网、或在回环之外监听，都不是默认行为。
- **错误契约。** 每个工具在敌意输入下都返回结构化错误信封，绝不向 transport 抛异常（由 `tests/unit/test_tool_fault_contract.py` 与 `tests/unit/test_tool_contract_surface.py` 全量强制）。
- **凭据脱敏。** 事件与事故日志会对 `authorization`、`token`、`secret`、`password`、`api_key` 等字段做脱敏（见 `error_boundary`）。日志或事件里泄漏明文凭据属于漏洞。

## 报告范围

### 属于范围（请上报）

- 越过 `local_full_access` 只读策略或 `agent_*` 审批门执行写操作；
- 任意命令执行、代码注入或参数注入（例如绕过设备序列号 / 包名的正则校验）；
- token 绕过，或在回环之外的意外监听 / 转发；
- 事故日志、telemetry 或 API 响应中泄漏凭据或密钥；
- 使常驻状态无界增长、可被远端触发的拒绝服务；
- 让某个工具向 transport 抛出异常而非返回错误信封。

### 不属于范围（预期行为，请隔离）

- **被分析样本自身的恶意行为。** 调试器会真实执行目标代码——这是工具的用途。请在可随时丢弃的隔离环境中运行执行端（见 README 的「隔离部署」与「隔离部署」两节）。
- 在**未隔离**环境里让样本攻破宿主：这是部署方的责任，不是本工具的缺陷。
- **反检测缺失。** 隐藏桌面解决的是「不干扰你的桌面」，不是反检测；样本仍可能识别虚拟化、调试器或非默认桌面。
- 缺失可选后端时的降级行为（缺 CLI 只降级，不算漏洞）。
- 上游组件（IDA、x64dbg、Playwright、mitmproxy 等）自身的漏洞——请向对应上游上报。
- 需要本机已被攻陷或需要物理访问才能成立的攻击。

## 安全部署清单

- 把执行端放进可随时丢弃的隔离环境（专用物理机、Hyper-V/VMware 快照或 Windows Sandbox）；
- 用专用低权限账户运行，不共享宿主目录、剪贴板与凭据；
- 默认断网，确需联网走白名单代理；
- 生产/共享环境用只读部署（`local_full_access: false`），按需再逐工具放开；
- Web 监控台只留在回环 + token，不要转发到局域网；
- 每次分析未知样本后回滚快照或重装，不要在同一环境里连续分析多个未知样本；
- 只分析你拥有或获得明确授权的样本。
